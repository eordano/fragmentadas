# Memorias Fragmentadas Website

Static website for the writing project.

- `master` branch: proposal 1, with top navigation
- `bottom-nav` branch: proposal 2, with bottom navigation

## Build

```bash
uv run --script deploy # will handle dependencies
```

## Deploy

Build first, then deploy `dist/` to a statically served path.
